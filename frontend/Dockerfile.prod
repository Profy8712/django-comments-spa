# ---- build stage ----
FROM node:20-alpine AS build
WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

# ---- export dist ----
FROM alpine:3.20 AS export
WORKDIR /export
COPY --from=build /app/dist ./dist
